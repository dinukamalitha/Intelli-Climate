import streamlit as st
from styles import apply_custom_css
from data_loader import load_models_and_data, get_helper_variables
from ui_components import render_sidebar, render_header, render_climate_zones, render_predictor, render_explorer

# ── PAGE CONFIG ────────────────────────────────────────────────────────────────
st.set_page_config(
    page_title="Sri Lanka Climate Intelligence",
    page_icon="🌧️",
    layout="wide",
    initial_sidebar_state="expanded"
)

# ── APPLY STYLES ──────────────────────────────────────────────────────────────
apply_custom_css()

# ── LOAD DATA ──────────────────────────────────────────────────────────────────
M = load_models_and_data()
helpers = get_helper_variables(M)

# ── SIDEBAR ────────────────────────────────────────────────────────────────────
render_sidebar(M, M['df'], helpers)

# ── HEADER ─────────────────────────────────────────────────────────────────────
render_header()

# ── TABS ───────────────────────────────────────────────────────────────────────
tab1, tab2, tab3 = st.tabs([
    "🗺️  Climate Regions",
    "🌧️  Weather Forecast",
    "📈  City Explorer",
])

with tab1:
    render_climate_zones(M['station'], M['zone_names'])

with tab2:
    render_predictor(M, helpers)

with tab3:
    render_explorer(M, helpers)
